services:
  redis:
    image: redis:latest
    container_name: redispubsub
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: stock-sim
    env_file:
      - .env
    environment:
      - REDIS_ADDR=redispubsub:6379
    ports:
      - "8080:8080"
    depends_on:
      - redis